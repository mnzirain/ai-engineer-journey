apiVersion: v1
kind: Service
metadata:
  name: enterprise-ai-service
  namespace: enterprise-ai

spec:
  selector:
    app: enterprise-ai-gateway

  ports:
    - port: 80
      targetPort: 8000

  type: NodePort